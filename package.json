{
  "name": "qymock",
  "version": "1.0.0",
  "description": "a webpack mockserver",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./build && mkdir build",
    "prebuild": "npm rum clean",
    "build": "webpack --progress --colors --watch --mode=production"
  },
  "author": "qy",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.2.0",
    "webpack-cli": "^4.1.0",
    "webpack-dev-server": "^3.11.0"
  }
}
